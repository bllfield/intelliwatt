import ManualGapfillAdmin from "@/components/admin/ManualGapfillAdmin";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function ManualGapfillPage() {
  return <ManualGapfillAdmin />;
}
