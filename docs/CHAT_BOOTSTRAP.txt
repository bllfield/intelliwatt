This is the new chat. Use these instructions as your brain. Don’t recreate them, just follow them, and continue the project from the exact state we are at.

🔥 INTELLIWATT · SMT JWT STATUS BOOTSTRAP (UPDATED 2025-11-22)
You are ChatGPT inside the IntelliWatt/IntelliPath project.

Follow docs/CHATGPT_HOUSE_RULES.md:
- One step per answer
- Always say where to run things (PowerShell, droplet, Vercel UI, Cursor Workbench)
- Anything Cursor edits must be a single Cursor Agent Block
- No extra fluff; only what’s required to make it function
- Default model: GPT-5 Codex
- Wait for me to say “done” after each step
- Before asking the user to add or change anything, search the repo. If the value or script already exists, paste it directly—do not tell the user to create what’s already here.
- If a command needs root (or deploy), spell out the transition steps every time (e.g., `exit` back to root, or `sudo -iu deploy` from root) before giving the command—never assume the user is already at the right prompt.
- All time-series modeling uses canonical UTC 15-minute timestamps; weather is daily station lookup by `dateKey`; solar will be a separate tile/grid layer later.
- Usage/simulation layer naming must follow `docs/USAGE_LAYER_MAP.md` and `modules/usageSimulator/kinds.ts` (`IntervalSeriesKind`).

========================================
SHARED SIM → DROPLET (SHIPPED 2026-03-27, EXTENDED)
========================================

- **One runner:** `scripts/droplet/sim-job-run.ts <jobKind> <id>` remains for droplet workers; `jobKind` can be **`gapfill_compare`** (legacy/rollback path) or **`past_sim_recalc`** (active for recalc jobs).
- **Webhook:** `deploy/droplet/webhook_server.py` still handles **`POST /trigger/smt-now`** for both reasons above.
- **Gap-Fill canonical lab path:** admin now uses canonical recalc/read actions (`lookup_source_houses`, `replace_test_home_from_source`, `save_test_home_inputs`, `run_test_home_canonical_recalc`) and the same saved artifact family as `GET /api/user/usage/simulated/house`; droplet compare remains legacy rollback diagnostics only.
- **Past sim recalc enqueue:** `dispatchPastSimRecalc` (`modules/usageSimulator/pastSimRecalcDispatch.ts`) calls the same **`recalcSimulatorBuild`** as before, either inline or via **`SimDropletJob`** + droplet worker (`runPastSimRecalcQueuedWorker`). User **`POST /api/user/simulator/recalc`** returns **`executionMode: "droplet_async"`** + **`jobId`**; poll **`GET /api/user/simulator/recalc?jobId=`**. Admin inspect **`GET /api/admin/simulation-engines?...&pastRecalcJobId=`** for status. **`PAST_SIM_RECALC_INLINE=true`** or **`SIM_DROPLET_EXECUTION_INLINE=true`** forces inline execution on Vercel.
- **Shared pipeline (Gap-Fill):** canonical compare path recalcs Past once with validation-only keys, reads from canonical saved artifact family, then applies baseline-vs-accuracy projection only (no compare-only simulator as canonical truth).

========================================
GAPFILL + SIM USAGE SHARED MODULES (CANONICAL)
========================================

Audit baseline (2026-03-13): all GapFill/simulation tests and admin tools must use shared modules below; do not duplicate business logic in routes/tests/tools.

1) Core simulation orchestration:
- `modules/usageSimulator/service.ts`
- `modules/simulatedUsage/simulatePastUsageDataset.ts`
- `modules/simulatedUsage/engine.ts`
- `modules/simulatedUsage/pastDaySimulator.ts`

2) Past identity/hash/window/cache:
- `modules/usageSimulator/windowIdentity.ts`
- `modules/usageSimulator/pastCache.ts`
- `modules/weather/identity.ts`
- `lib/usage/actualDatasetForHouse.ts` (`getIntervalDataFingerprint`)
- `modules/simulatedUsage/simulatePastUsageDataset.ts` (`getUsageShapeProfileIdentityForPast`)

3) Build/dataset/day-grid shared helpers:
- `modules/usageSimulator/build.ts`
- `modules/usageSimulator/dataset.ts`
- `modules/usageSimulator/pastStitchedCurve.ts`
- `lib/time/chicago.ts`

4) Canonical interval source + persisted artifact reads:
- `modules/realUsageAdapter/actual.ts`
- `lib/usage/actualDatasetForHouse.ts`
- `lib/usage/resolveIntervalsLayer.ts`
- `lib/usage/intervalSeriesRepo.ts`

5) Usage-shape profile dependencies for sim:
- `modules/usageShapeProfile/repo.ts`
- `modules/usageShapeProfile/derive.ts`
- `modules/usageShapeProfile/actualIntervals.ts`
- `modules/usageShapeProfile/autoBuild.ts`

6) Admin/shared tooling modules:
- `lib/admin/gapfillLab.ts`
- `lib/admin/gapfillLabPrime.ts`
- `lib/admin/simulatorDiagnostic.ts`
- `modules/usageSimulator/profileDisplay.ts`
- `modules/usageSimulator/simulationDataAlerts.ts`
- `modules/usageSimulator/repo.ts`

Mandatory enforcement rules:
- Routes, pages, admin tools, and tests may orchestrate and format only; reusable business logic must stay in shared modules.
- Tests must validate via the same shared modules or route contracts; no test-only duplicate business math.
- Rebuild/parity/diagnostic paths must be explicit; default reads should be artifact/cache-first where possible.
- If functionality already exists in one of the shared modules above, Cursor must use or extend that module instead of writing similar logic elsewhere.
- If a new reusable module is created in the future, it must be added to this canonical registry in all three docs immediately.
- If this task creates any new reusable shared module or shared helper, Cursor must update the canonical registry in all three docs in the same change before finishing.
- Any new route/tool/test that depends on GapFill or simulated usage must first check this canonical registry before adding logic.
- No duplicate code is allowed for date/window logic, weather identity logic, interval source selection, artifact identity/hash logic, profile identity logic, simulation-day generation, stitched Past artifact building, or diagnostic orchestration.

Shared simulation architecture authority:
- Past Sim and GapFill compare use the same shared artifact identity/fingerprint and shared simulator logic path.
- Travel/vacant days are the only excluded ownership days for the shared artifact fingerprint.
- Test days remain included in the shared artifact population and are only selected by GapFill for scoring against actual usage.
- GapFill is scoring/reporting only. It must not create a compare artifact, create a compare-mask fingerprint, change artifact identity, or rebuild simulated intervals locally; it reads shared simulator output via cached artifact restore or fresh shared-build path.
- GapFill core/default compare scoring now uses selected-day fresh shared execution (`compareFreshMode=selected_days`) while display data remains artifact-backed.
- GapFill heavy diagnostics can opt into full-window shared fresh scoring (`compareFreshMode=full_window`); this proof path is non-default.
- Artifact fingerprint ownership and usage-shape profile identity behavior stay unchanged in this step; deferred identity/profile changes remain out of scope.
- Shared weather truth is owned by `loadWeatherForPastWindow`.
- `loadWeatherForPastWindow` must reuse persisted non-stub `ACTUAL_LAST_YEAR` daily weather rows when the canonical date window is already covered; do not trigger redundant weather backfill/API work in that case.
- Backfill/stub repair is only for missing or `STUB_V1` dates, and provenance must stay truthful (`actual_only`, `mixed_actual_and_stub`, or `stub_only`) because both Past Sim and GapFill read this same shared weather path.
- Current runtime note: compare-run persistence is now live (`GapfillCompareRunSnapshot`) and `compare_core` now returns `compareRunId`, `compareRunStatus`, and `compareRunSnapshotReady`.
- Current runtime note: staged heavy snapshot readers now exist:
  - `compare_heavy_manifest`
  - `compare_heavy_parity`
  - `compare_heavy_scored_days`
- Current runtime note: canonical heavy follow-up is now snapshot-read-only over `compareRunId`; reader stages do not own sim/artifact/weather logic.
- Current runtime note: admin stabilization polish is now in place for staged readers:
  - stage-scoped snapshot-reader retry,
  - compact per-stage snapshot-reader history in Last Attempt Debug,
  - clearer snapshot-reader stage labeling in payload/debug views.
- Current runtime note: legacy `compare_heavy` compatibility may still exist, but it should not be treated as the normal path.
- Route-level or tool-level simulation math is not allowed when shared simulator output already exists.
- Authoritative shared simulator call chain:
  - `getPastSimulatedDatasetForHouse`
  - `simulatePastUsageDataset`
  - `loadWeatherForPastWindow`
  - `buildPastSimulatedBaselineV1`
  - `buildCurveFromPatchedIntervals`
  - `buildSimulatedUsageDatasetFromCurve`

Simulation modeling authority:
- Canonical simulation-logic reference is `docs/USAGE_SIMULATION_PLAN.md`.
- This bootstrap should stay short and operational; do not redefine modeling logic here.
- Architecture cleanup is complete for shared-artifact alignment. Current focus is shared simulation-core accuracy.
- Current runtime subtask: maintenance-only follow-up (optional admin dedupe cleanup, optional legacy `compare_heavy` compatibility cleanup, optional observability/perf polish).
- Do not reopen Gap-Fill architecture refactor unless a real, current active-path bug is confirmed.
- Do not reopen artifact architecture cleanup unless a new active-path bug proves it is necessary.

Modeling mode split (summary):
- Observed-history reconstruction (Past Sim + GapFill compare): prioritize actual interval history + weather + weekday/weekend + time-of-day + similar-day matching.
- Overlay/delta mode: apply home/appliance/occupancy/HVAC/thermostat/pool/EV/envelope deltas on top of baseline.
- Synthetic/sparse-data mode (manual/new-build/low-history): prioritize declared home/appliance/occupancy details + weather + learned priors.

Home details collection (summary):
- Home details are required and normalized for every home.
- In observed-history reconstruction they are supportive context/priors/fallback, not the primary truth source when interval history is strong.
- In overlay and synthetic/sparse-data modes they are primary modeling inputs.

LEGACY / NON-AUTHORITATIVE (historical drift notes retained):
- Older notes in previous chats/plans may mention unresolved shared wiring items for diagnostics/identity helpers. Treat those as historical context unless a new active-path regression is confirmed in current code.

========================================
DATABASE CONNECTION (DO NOT COMMIT SECRETS)
========================================

Do not paste real DB passwords into this file. Use placeholders in docs, and store real values only in:
- Vercel Environment Variables (Preview + Production)
- Local `.env.local` (gitignored) or PowerShell session env vars

```
DATABASE_URL="postgresql://doadmin:<PASSWORD>@db-postgresql-nyc3-37693-do-user-27496845-0.k.db.ondigitalocean.com:25060/intelliwatt_dev?sslmode=require"
```

- Store real values in Vercel / `.env.local` (gitignored).
- Droplet command (if applicable):
  ```bash
  sudo nano /etc/environment    # append the two lines
  source /etc/environment
  ```
  Restart services afterwards (`sudo systemctl restart <service>`).
- Prisma schema already expects `url` + `directUrl`; leave them in place.
- Prisma Studio uses `DATABASE_URL`, so it now goes through PgBouncer. Close Studio when finished.
- Never assume you are already the correct user on the droplet. Always show the login step and, when needed, switching users (`sudo -iu deploy`) plus the `cd` command before running anything.

Keeper Cleanup Runbook (Chat executes every command):
- Confirm a DO snapshot/pg_dump before modifying the production database.
- Run (Windows Command Prompt, repo root): 
  1. `npx prisma db execute --file "scripts\sql\bulk_archive_non_keeper_users.sql" --schema prisma\schema.prisma`
  2. `npx prisma db execute --file "scripts\sql\delete_non_keeper_users.sql" --schema prisma\schema.prisma`
  3. Optional polish: `npx prisma db execute --file "scripts\sql\delete_non_keeper_entries.sql" --schema prisma\schema.prisma` and `npx prisma db execute --file "scripts\sql\delete_non_keeper_smt_authorizations.sql" --schema prisma\schema.prisma`
  4. Reseed keepers: `node scripts\dev\seed-keeper-users.mjs`
  5. Verify: `npx prisma db execute --stdin --schema prisma\schema.prisma` then `SELECT COUNT(*) FROM "User";` (expect `5`).
- If verification fails, stop and investigate—do not proceed with additional seeding.

========================================
SMT AUTH STATE (DO NOT REGRESS)
========================================

Smart Meter Texas market notice SMT-M-A051425-10 (May 14, 2025):
- FTPS and non-JWT API flows are decommissioned.
- ONLY SFTP (SSH) + REST API with JWT tokens are valid.

Canonical SMT REST auth (2025+):

Token endpoint:
- POST ${SMT_API_BASE_URL}/v2/token/

Body (JSON):
{
  "username": "<SMT_USERNAME>",   // API Service ID
  "password": "<SMT_PASSWORD>"    // API Service ID password
}

Response:
- statusCode: 200
- accessToken: "<JWT>"
- tokenType: "Bearer"
- expiresIn: 3600
- issuedAt / expiresAt

All SMT REST calls must send:
- Authorization: Bearer <accessToken>

DO NOT:
- Use /oauth/token
- Use OAuth2 client_credentials
- Use SMT_JWT_CLIENT_ID / SMT_JWT_CLIENT_SECRET
- Suggest FTPS-only API flows

========================================
WORKING PROD CREDS (VERIFIED)
========================================

We have successfully tested on the SMT droplet:

- SMT_API_BASE_URL = https://services.smartmetertexas.net
- SMT_USERNAME     = INTELLIPATH
- SMT_PASSWORD     = (the SMT API Service ID password set in the SMT portal)

Droplet test script (already created and verified):

- Host: intelliwatt-smt-proxy
- Script: /home/deploy/smt_token_test.sh

This script does:

SMT_API_BASE_URL="${SMT_API_BASE_URL:-https://services.smartmetertexas.net}"
SMT_USERNAME="${SMT_USERNAME:-INTELLIPATH}"

Prompts for password, then:

POST https://services.smartmetertexas.net/v2/token/
with { username: SMT_USERNAME, password: SMT_PASSWORD }

and returns a 200 with:
- accessToken (JWT)
- tokenType: "Bearer"
- expiresIn, issuedAt, expiresAt

This is the **canonical live SMT auth test** (droplet IP is whitelisted).

========================================
VERCEL BACKEND ROUTE BEHAVIOR
========================================

Vercel IPs are NOT whitelisted by SMT, so calling SMT directly from Vercel times out.

Because of that:

- Route: GET /api/admin/smt/token
- Behavior:
  - Enforces x-admin-token via ADMIN_TOKEN (same as other admin routes)
  - Returns a JSON stub with HTTP status 501:
    {
      ok: false,
      status: 501,
      message: "SMT JWT token must be tested from the whitelisted SMT droplet, not from Vercel.",
      instructions: {
        dropletHost: "intelliwatt-smt-proxy",
        scriptPath: "/home/deploy/smt_token_test.sh",
        note: "This route intentionally returns 501 because SMT only whitelists the droplet IP. Use the droplet script for live JWT verification."
      },
      integration: {
        endpoint: "https://services.smartmetertexas.net/v2/token/",
        authFlow: "POST with { username: SMT_USERNAME, password: SMT_PASSWORD }",
        usernameEnv: "SMT_USERNAME=INTELLIPATH",
        baseUrlEnv: "SMT_API_BASE_URL=https://services.smartmetertexas.net"
      }
    }

PowerShell test pattern (from my laptop) is ONLY to confirm this admin route returns the stub, NOT to talk to SMT directly:

$BASE_URL    = "https://intelliwatt.com"
$ADMIN_TOKEN = "<64-char ADMIN_TOKEN>"

Invoke-RestMethod `
  -Method Get `
  -Uri "$BASE_URL/api/admin/smt/token" `
  -Headers @{ "x-admin-token" = $ADMIN_TOKEN }

Expected: 501 with the JSON instructions above.

========================================
WHAT THE NEXT GPT SHOULD DO
========================================

- Assume SMT JWT auth **already works** using:
- SMT_API_BASE_URL = https://services.smartmetertexas.net
- SMT_USERNAME     = INTELLIPATH
- SMT_PASSWORD     = configured in SMT portal
- NEVER reintroduce /oauth/token, client_credentials, or FTPS-only flows.
- If SMT auth needs to be verified, instruct me to:
  - SSH to intelliwatt-smt-proxy
  - Run /home/deploy/smt_token_test.sh
- Treat /api/admin/smt/token as a documentation/diagnostic stub, not as a live SMT proxy, unless we explicitly decide to add a droplet-backed proxy in a future step.

Droplet sync enforcement (EFL / SMT / helpers):
- Whenever you create or modify any file under `deploy/droplet/**` or any droplet-only helper script/config:
  - After committing and pushing to `main`, you MUST tell me explicitly that the droplet needs a pull + post-pull sync.
  - Include a concrete command block for the droplet:
    - From `deploy`: 
      - `cd /home/deploy/apps/intelliwatt`
      - `git pull origin main`
      - `sudo bash deploy/droplet/post_pull.sh`
  - If a change does **not** require a droplet pull (Vercel-only or docs-only), say that clearly: “No droplet update needed for this push.”

========================================
CURSOR WORKFLOW: “DONE” MEANS EXECUTED
========================================

- I will confirm a Cursor Agent Block only after I run it. Questions or follow-ups are not confirmation.
- NEVER assume a block executed until I explicitly reply with **“done”**.
- Stay on the current step (and refine that block if needed) until you hear “done”.
- Always state exactly where to run commands (Cursor Workbench, droplet SSH, Vercel UI, etc.).
- One logical step per answer; do not queue future steps in advance.
- SMT integrations remain droplet-only. Vercel and the browser must never call SMT directly.

========================================
DROPLET SCRIPTS · GIT AS SOURCE OF TRUTH
========================================

SIMPLE DROPLET SYNC (CANONICAL EXAMPLE)
---------------------------------------

From Windows PowerShell, in the repo root (`intelliwatt-clean`), push the updated
webhook server to the droplet with:

  scp.exe -i "$env:USERPROFILE\.ssh\intelliwatt_win_ed25519" ".\deploy\droplet\webhook_server.py" root@64.225.25.54:/home/deploy/webhook_server.py

To push a different file, keep the same pattern and change only the local path
and the remote destination.

DROPLET GIT PULL (CANONICAL FLOW)
---------------------------------

When pulling changes directly on the droplet:

  sudo -iu deploy
  cd /home/deploy/apps/intelliwatt
  git status -sb
  git pull origin main
  sudo bash deploy/droplet/post_pull.sh


sudo journalctl -u smt-webhook.service -n 200 -f
===========================================================
MASTER DB MIGRATIONS (DRIFT-SAFE): DEV FIRST, THEN PROD
===========================================================

Why this exists:
- This repo has had schema drift historically. Running `prisma migrate deploy` directly on PROD can fail or do the wrong thing.
- Our canonical workflow is: **reset/clean DEV**, apply all migrations there, verify, then apply the same migration(s) to PROD using **direct SQL execution**.

-----------------------------------------------------------
STEP A — DEV (LOCAL): reset + apply all migrations
-----------------------------------------------------------

CANONICAL: We run DEV and PROD migrations from the droplet repo checkout.
This keeps “one environment” (Linux + repo) consistent and matches how we actually deploy.

Where: Droplet SSH session. Start from `root`, then switch to `deploy`.

0) Become `deploy` and go to the repo:
     sudo -iu deploy
     cd /home/deploy/apps/intelliwatt
     pwd
     whoami

Expected:
- `pwd`  -> `/home/deploy/apps/intelliwatt`
- `whoami` -> `deploy`

1) Pull latest code (so migrations are present):
     git status -sb
     git pull origin main

2) Point DATABASE_URL at DEV *for this shell session only*:
     export DATABASE_URL='postgresql://doadmin:<PASSWORD>@<HOST>:25060/intelliwatt_dev?sslmode=require'

3) Reset DEV to a clean slate and apply all migrations:
     npx prisma migrate reset --force --schema prisma/schema.prisma
     npx prisma migrate deploy --schema prisma/schema.prisma

4) Verify the new table exists (example for HomeSavingsSnapshot):
   IMPORTANT: `prisma db execute` is **not meant for returning data**; it only reports success/failure.
   For a verification that prints results, use `psql`:

     psql "$DATABASE_URL" -c 'SELECT current_database() AS db, to_regclass(''public."HomeSavingsSnapshot"'') AS table_name;'
     psql "$DATABASE_URL" -c '\d+ "HomeSavingsSnapshot"'

Expected:
- `table_name` is `HomeSavingsSnapshot` (not null)
- `\d+` output shows the columns + indexes + constraints for the table

-----------------------------------------------------------
STEP B — PROD (DROPLET): execute SQL for the specific migration
-----------------------------------------------------------

Pre-req: Take a DO snapshot first (MANDATORY).

Where: Droplet SSH session. Start from `root`, then switch to `deploy`.

0) Become `deploy` and go to the repo:
     sudo -iu deploy
     cd /home/deploy/apps/intelliwatt
     pwd
     whoami

Expected:
- `pwd`  -> `/home/deploy/apps/intelliwatt`
- `whoami` -> `deploy`

1) Pull latest code (so the migration file exists):
     git status -sb
     git pull origin main

2) Point DATABASE_URL at PROD *for this shell session only*.
   (We never commit secrets; do NOT write real passwords into repo files.)
     export DATABASE_URL='postgresql://doadmin:<PASSWORD>@<HOST>:25060/intelliwatt?sslmode=require'

3) Execute the migration SQL directly (example: HomeSavingsSnapshot):
     npx prisma db execute --schema prisma/schema.prisma --file prisma/migrations/20260217000000_home_savings_snapshot/migration.sql

4) Verify (prints results via `psql`):
     psql "$DATABASE_URL" -c 'SELECT current_database() AS db, to_regclass(''public."HomeSavingsSnapshot"'') AS table_name;'
     psql "$DATABASE_URL" -c '\d+ "HomeSavingsSnapshot"'

Expected:
- `table_name` is `HomeSavingsSnapshot` (not null)
- `\d+` output shows the columns + indexes + constraints for the table

Hard rules:
- Do NOT run `npx prisma migrate reset` on PROD (ever).
- Do NOT run `npx prisma migrate dev` on PROD (ever).
- PROD gets the migration via `prisma db execute --file ...` only, after DEV has been proven clean.

NOTE: Common droplet pitfalls
-----------------------------

- Do NOT paste Windows paths (e.g. `C:\...`) into droplet bash.
- Do NOT use PowerShell syntax on the droplet (e.g. `$env:DATABASE_URL=...`). In bash use:
  - `export DATABASE_URL="..."`
- Do NOT run `npx prisma ...` from `~` or any directory that is NOT the repo root.
  Symptom: it asks to install `prisma@<latest>` and/or fails with:
  - `Could not load --schema ... prisma/schema.prisma: file or directory not found`
- If `git pull` errors with `Permission denied (publickey)` you are running under a user
  without GitHub SSH auth (often `root`). Switch to `deploy` (`sudo -iu deploy`) or fix SSH keys.

After `post_pull.sh` completes, the droplet nginx/site + systemd config for EFL pdftotext is reapplied from the repo, core services are restarted if present, and the EFL helper health is checked at:

  curl -i https://efl-pdftotext.intelliwatt.com/health
Optional: run `git fetch origin` followed by `git status -sb`; it should show `## main...origin/main`.

GAP-FILL COMPARE CANONICAL PATH (INLINE)
----------------------------------------

Gap-Fill compare now uses one canonical in-request path:
1) run canonical recalc for Past with travel/vacant + validation-only date keys
2) read canonical simulated output from `getSimulatedUsageForHouseScenario` (same family as `/api/user/usage/simulated/house`)
3) generate baseline projection (validation keys remain actual) and compare projection sidecar (modeled values for validation keys only)

Validation-day selector model (canonical):
- Admin page has two selectors:
  - System-wide user-facing default (`userDefaultValidationSelectionMode`) for future user recalcs only.
  - Admin-lab per-run mode (does not implicitly change system default).
- Changing system default never rewrites existing artifacts; old artifacts keep their saved effective mode/output until recalculated.

Any droplet `gapfill_compare` route remains legacy fallback only and must not be treated as canonical truth ownership.

DROPLET LOG TAILING (LAST 5 MINUTES)
------------------------------------

On the droplet, run as `root` or prefix with `sudo`:


FROM ROOT
cd /home/deploy/apps/intelliwatt
git pull origin main
sudo bash deploy/droplet/post_pull.sh
sudo cp deploy/droplet/webhook_server.py /home/deploy/webhook_server.py
sudo systemctl restart smt-webhook.service
sudo systemctl restart smt-ingest.service
sudo systemctl restart green-button-upload.service
sudo systemctl restart efl-pdftotext.service

sudo journalctl -u smt-webhook.service -n 200 -f
sudo journalctl -u green-button-upload.service -n 200 -f
sudo journalctl -u efl-pdftotext.service -n 200 -f


# (Optional) confirm it came back clean
sudo journalctl -u green-button-upload.service --since "5 minutes ago" --no-pager

- ``sudo journalctl -u smt-webhook.service -f
- `sudo journalctl -u smt-webhook.service --since "5 minutes ago" --no-pager`
- `sudo journalctl -u smt-ingest.service --since "5 minutes ago" --no-pager`

- All droplet-side SMT scripts live in the repo and must be edited via Cursor:
  - `deploy/smt/fetch_and_post.sh`
  - `deploy/droplet/webhook_server.py`
  - `deploy/droplet/run_webhook.sh`
  - `deploy/droplet/smt_token_test.sh`
- When droplet behavior needs to change:
  1. Provide a Cursor Agent Block that edits these repo files.
  2. Wait for Brian to execute the block and reply “done”.
  3. Instruct Brian to sync the updated files from Windows using `scp` with the key `%USERPROFILE%\.ssh\intelliwatt_win_ed25519` (targets `root@64.225.25.54`).
  4. Have Brian restart `smt-webhook.service` / `smt-ingest.service` and tail logs via `journalctl`.
- Assume SMT SFTP layout:
  - Remote: `/adhocusage` and `/EnrollmentReports` on `ftp.smartmetertexas.biz`.
  - Droplet inbox: `/home/deploy/smt_inbox`.
  - Files may be `*.csv` or `*.CSV.*.asc`; ingest logic must handle both.
- SMT remains droplet-only: Next.js/Vercel and the browser must never call SMT directly; tests run through droplet scripts.

Where earlier content in this file conflicts with the following, the following section takes precedence.

AUTHORITATIVE SIMULATOR ARCHITECTURE OVERRIDE

This section overrides any older contradictory guidance in this file.

This override applies to every simulator execution mode and entrypoint, including initial run, cold start, cold build, cache miss rebuild, allow_rebuild, refresh, explicit recalc, admin canonical recalc, user-triggered rebuild, artifact refresh, artifact ensure, snapshot-producing rebuilds, and any future renamed equivalent. No execution mode is exempt.

User Past Sim and GapFill may start with different inputs, but after input normalization they must enter the exact same shared simulator producer path.

There is only one pre-DB simulator producer path.

Producer labels must include:
- TRAVEL_VACANT
- TEST

Fingerprint rule:
- exclude only TRAVEL_VACANT days from the usage fingerprint
- keep TEST-day actual usage included in the usage fingerprint

After simulated day outputs are derived and stored:
- stitch uses ACTUAL + TRAVEL_VACANT simulated days only
- compare uses TEST simulated days only against actual interval data for those same days

TEST simulated days do not go into the stitched Past chart.

User Past Sim and GapFill must read the same stored simulator truth and the same stored compare truth. GapFill may add more analytics, but not a separate truth path.

Fresh diagnostics are diagnostics only. They do not replace stored compare truth.

In selected_days fresh diagnostics mode, scored day totals must come from canonical simulator-owned day totals, not interval re-sums.

Whenever a new simulator run produces new simulated outputs for the same scope and identity, stale simulated data from prior runs must be cleared, replaced, or fully overwritten. Old test-day outputs, old travel/vacant outputs, old tuning outputs, and old calculation-version outputs must not remain mixed into the current stored result set.

If a future chat proposes separate pre-DB producer paths for user Past Sim and GapFill, claims that a specific mode like cold build, refresh, or recalc is exempt, or allows stale simulated data from prior runs to remain mixed into current stored outputs, that proposal is off-plan and must be rejected.

AUTHORITATIVE FAIL-CLOSED TRUTH OVERRIDE

This section overrides any older contradictory guidance in this file.

Truth-preserving fallbacks are allowed. Truth-substituting fallbacks are not.

Allowed examples:
- cached weather in DB falling back to a fresh weather API for the same weather truth
- primary weather provider falling back to a backup weather provider when the backup still returns correct weather truth for the same request
- equivalent trusted source-layer fallback that preserves the same identity and same truth

Forbidden fallbacks include anything that can return bad, stale, mixed, substitute, guessed, zero-filled, partial, or misleading truth.

If the correct simulator truth, compare truth, artifact truth, or stored-output truth cannot be proven correct, the system must fail with an explicit failure status, code, and message.

When exact identity is required, failure to resolve exact identity must not fall back to latest artifact, latest by scenario, compatible identity, or any substitute source.

Compare truth must remain stored-output-backed or artifact-backed. Missing compare simulator totals must not be converted into numeric 0 values and shown as valid compare output.

Fresh diagnostics are diagnostics only. They must not replace compare truth. If canonical fresh scored day totals are required and missing, diagnostics must fail explicitly.

Whenever a new simulator run produces new simulated outputs for the same scope and identity, prior simulated outputs must be cleared, replaced, or fully overwritten so stale outputs are not mixed into the current result set.

If a fallback preserves the same truth, it is allowed. If it can produce bad data or misleading data, it is forbidden.
