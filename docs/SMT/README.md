> **OVERRIDE NOTICE (2025-09-13 and later):**  
> The **authoritative** instructions for SMT integration now live in `docs/00_OVERRIDES/SMT_JWT_UPGRADE.md`.  
> If anything below conflicts with that file, the override document takes precedence.

> **SMT coverage unification (2026):** Interval completeness, window lag, heal orchestration, and 96/96 rules are being consolidated per `docs/SMT_UNIFICATION_PLAN.md`. Phased implementation: `docs/SMT_UNIFICATION_PHASE_PROMPTS.md`. New agent chat: `docs/SMT_UNIFICATION_AGENT_BOOTSTRAP.md`.
