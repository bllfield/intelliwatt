> **OVERRIDE NOTICE (2025-09-13 and later):**  
> The **authoritative** instructions for SMT integration now live in `docs/00_OVERRIDES/SMT_JWT_UPGRADE.md`.  
> If anything below conflicts with that file, the override document takes precedence.
