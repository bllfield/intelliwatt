# IntelliWatt EFL Fetch Proxy env (droplet)
#
# This file is copied to /home/deploy/.efl-fetch-proxy.env by apply_efl_fetch_proxy.sh
# if the live file does not already exist.

HOST=127.0.0.1
PORT=8088

# Shared secret (recommended). Vercel will send:
#   Authorization: Bearer <EFL_FETCH_PROXY_TOKEN>
EFL_FETCH_PROXY_TOKEN=change-me

# Cap fetched bytes (default 15MB). Prevents accidental huge downloads.
EFL_FETCH_PROXY_MAX_BYTES=15728640

# Optional allowlist of host suffixes (comma-separated). When set, ONLY these hosts are allowed.
# Example:
# EFL_FETCH_PROXY_ALLOW_HOSTS=ohm-gridlink.smartgridcis.net,pp-gridlink.paylesspower.com
EFL_FETCH_PROXY_ALLOW_HOSTS=

