[Service]
# Force a single source of truth for env vars for smt-webhook.
# Clear any previously-configured EnvironmentFile entries (including host-local overrides)
# to prevent token mismatches and "export KEY=VALUE" parsing issues.
EnvironmentFile=
EnvironmentFile=/etc/default/intelliwatt-smt

Restart=always
RestartSec=2

