HOST=127.0.0.1
PORT=8088

# Shared secret (optional but recommended).
# If set, clients must send:
#   Authorization: Bearer <EFL_FETCH_PROXY_TOKEN>
EFL_FETCH_PROXY_TOKEN=change-me

# Safety: cap downloads (bytes). Default 15MB.
EFL_FETCH_PROXY_MAX_BYTES=15728640

# Safety: optional allowlist of host suffixes (comma-separated).
# Example: ohm-gridlink.smartgridcis.net,pp-gridlink.paylesspower.com
# When set, ONLY these hosts are allowed.
EFL_FETCH_PROXY_ALLOW_HOSTS=

